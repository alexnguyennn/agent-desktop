# Preserve any parent directory environment, then load this checkout's private
# local build destination when configured. The path itself lives in private.env.
source_up_if_exists
dotenv_if_exists private.env
